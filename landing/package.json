{
  "name": "lingguang-landing",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/gen-version.mjs && vite",
    "build": "node scripts/gen-version.mjs && tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "deploy": "bash deploy.sh"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.4.1",
    "typescript": "^5.8.3",
    "vite": "^6.3.2"
  }
}
